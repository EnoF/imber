(function autoCompleteWidgetSpecsScope(sinon) {
  'use strict';

  describe('<auto-complete> specs', function autoCompleteWidgetSpecs() {
    it('should instantiate with the provided options', function providedOptions() {

    });
  });
}(window.sinon));
