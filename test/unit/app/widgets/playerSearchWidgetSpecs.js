(function playerSearchWidgetSpecsScope(angular) {
  'use strict';

  describe('<player-search> specs', function playerSearchWidgetSpecs() {
    it('should emit the selected player', function emitSelectedPlayer() {
      // given

      // when

      // then

    });
  });
}(window.angular));
