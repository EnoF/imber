(function characterVMSpecs(sinon) {
  'use strict';

  describe('characterVM specs', function characterVMSpecs() {
    it('open action panel', function openActionPanel() {
      // given

      // when

      // then

    });
  });
}(window.sinon));
